现在进入完整 Context Form。

它会把一次 quest 启动拆成几个可审计模块：

- 左侧：任务目标、参考资料、材料、connector、DeepXiv、启动合同和运行约束
- 右侧：SetupAgent 或启动预览，用来检查即将交给 agent 的上下文
- 底部：创建项目动作和当前模板/状态提示

第一次使用时，不必展开所有高级项。先确认标题、目标、参考资料、运行约束和启动预览即可。
