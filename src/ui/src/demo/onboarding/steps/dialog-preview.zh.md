这里是创建项目前的最终预览。

它展示即将交给 agent 的启动上下文。创建前至少检查四件事：

- 任务是否与你真正想做的一致
- benchmark、本地路径、paper、baseline 是否正确
- 运行限制是否足够明确，不会让 agent 过度使用资源
- 预期产物是否具体，例如表格、图、报告、论文草稿或 PR

如果预览读起来不对，先回到左侧修改字段，或者切回 SetupAgent 让它帮你重写。
