### Details

- This view compresses the current project state into a fast briefing.
- The metric cards answer **how good the current branch is**.
- The `bash_exec` block answers **what command was actually run**, from which path, and what output came back.
