# 工作文档：接入 Qwen（通义千问）Runner

> 分支：`feature/luoxiang-add-qwen-api`
> 日期：2026-08-16

## 背景

把 DeepScientist 的 runner 接口改成走阿里云百炼（DashScope）的 Qwen API。
最初只提交了 Web UI 的改动（多了一个 Qwen 选择按钮），但实际跑不起来。
经同学排查 + 逐行核实，确认问题不在 Qwen 的实现本身，而在后端接线不完整。

## 同学诊断的 6 点，核实结果

| # | 诊断 | 结论 |
|---|---|---|
| 1 | `qwen_agent.py` HTTP POST 到 DashScope、`qwen.py` 子进程启动、连 3 个 MCP | ✅ 实现正确，无问题 |
| 2 | `self.runners` 遗漏 qwen → `get_runner("qwen")` 报 Unknown runner | ✅ 属实，web UI 直接报错的根因 |
| 3 | `binary="qwen"` 被当外部二进制检查 | ✅ 属实 |
| 4 | CLI 没导入/构造 QwenRunner，`register_builtin_runners()` 缺必填参数 | ✅ 属实，`ds run` 直接 TypeError |
| 5 | `default_runners()` 没有 qwen | ✅ 属实 |
| 6 | 配置校验误判 qwen 找不到二进制 | ✅ 属实（次要） |

## 根因（为什么“只有 UI 变了”）

`runners/metadata.py` 已经加了 qwen 的元数据，`list_builtin_runner_names()` 返回 5 个
（含 qwen），所以前端能列出按钮；但按钮背后真正执行的那条链路没接完。

四个具体 bug：

1. **`cli.py` 直接崩**：`register_builtin_runners()` 已把 `qwen_runner` 设为必填
   keyword-only 参数，但 CLI 调用时没传，`ds run`（任何 runner）报
   `TypeError: missing 1 required keyword-only argument: 'qwen_runner'`。
2. **daemon 没把 qwen 放进映射**：`self.runners` 只建了 codex/claude/kimi/opencode，
   `get_runner("qwen")` 查不到 → `Unknown runner qwen`。
3. **`binary="qwen"` 触发假阳性**：`_runner_binary_issue` 对 `binary="qwen"` 做
   `which("qwen")` 失败，在真正调 API 前就被拦下；实际 Qwen 是
   `python -m deepscientist.runners.qwen_agent` 子进程，不该有外部二进制。
4. **默认配置缺 qwen**：`default_runners()` 只有 4 个 runner，新装的 `runners.yaml`
   不含 qwen。

## 修复内容

### 1. `src/deepscientist/runners/metadata.py`
- `RunnerMetadata` 新增 `no_external_binary: bool = False` 字段。
- qwen 元数据置 `no_external_binary=True`（表示无 PATH 外部二进制）。

### 2. `src/deepscientist/daemon/app.py`
- `QwenRunner(...)` 的 `binary="qwen"` → `binary=""`。
- `self.runners` 元组加入 `"qwen"`。

### 3. `src/deepscientist/cli.py`
- 导入加入 `QwenRunner`。
- `run_command` 里构造 `qwen_runner`（`binary=""`）并传入
  `register_builtin_runners(...)`。

### 4. `src/deepscientist/config/models.py`
- `default_runners()` 补 `qwen` 条目：
  `enabled: False`、`binary: ""`、`model: qwen-plus`、
  `base_url: https://dashscope.aliyuncs.com/compatible-mode/v1`、
  `temperature: 0.2`、`env: {QWEN_API_KEY: ""}`。

### 5. `src/deepscientist/config/service.py`
- `_test_runners_payload` 对 `no_external_binary` 的 runner 跳过二进制存在性检查，
  直接进 `_probe_qwen_runner` 的 API 探测。

## 验证情况

- `python -m compileall src/deepscientist` ✅ 通过。
- 隔离验证 `metadata.py`（`list_builtin_runner_names()` 含 qwen、
  `qwen.no_external_binary=True`）与 `default_runners()`（含 qwen，`binary=''`、
  `model=qwen-plus`）✅。
- 未跑完整 `pytest`：当前 shell 的 Python 缺 `cryptography`/`pycryptodome`/`pytest`，
  `deepscientist.config` → `connector` → `weixin_support` 导入链在
  `from Crypto.Cipher import AES` 处即断（环境缺依赖，与本次改动无关）。
  需在装好依赖的环境补跑 `pytest`。

## 遗留事项（非代码问题）

Qwen 要真正调 API，需要配置 `QWEN_API_KEY`，二选一：

- 在 `~/DeepScientist/config/runners.yaml` 的 `qwen.env.QWEN_API_KEY` 填上；或
- 启动前 `export QWEN_API_KEY=...`。

旧 `runners.yaml` 无需重建：`load_runners_config()` 会 deep-merge `default_runners()`，
qwen 条目（含 `model: qwen-plus`）会自动补齐。
